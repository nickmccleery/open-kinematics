"""
Schema definitions for YAML parsing.

Defines marshmallow schemas for each suspension geometry type.
"""

# Schema definitions will be created here as needed
# For now, we'll use marshmallow_dataclass's automatic schema generation
